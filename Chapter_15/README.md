# webpack-demo

## Getting Started

First things first, install the all devDependencies:

```console
npm install
```

### And run the following in your terminal/console:

```console
npm run serve
```
